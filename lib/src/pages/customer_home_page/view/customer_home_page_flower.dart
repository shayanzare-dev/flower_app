import 'package:flutter/material.dart';

class CustomerHomePageFlower extends StatelessWidget {
  const CustomerHomePageFlower({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(title: const Text('Customer Home page flower'),backgroundColor:const Color(0xff04927c)),
      );
}
