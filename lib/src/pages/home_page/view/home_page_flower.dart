import 'package:flutter/material.dart';

class HomePageFlower extends StatelessWidget {
  const HomePageFlower({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(title: const Text('Home page flower')),
      );
}
